export enum GroupStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
}

export enum ParticipantStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  LEFT = 'left',
}

export enum ParticipantRole {
  HOST = 'host',
  MEMBER = 'member',
}
